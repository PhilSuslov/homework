type: object
properties:
  code:
    type: integer
    description: HTTP-код ошибки
    example: 400
  message:
    type: string
    example: Request data is invalid
  errors:
    type: array
    items:
      type: object
      example: user_uuid
    message:
      type: string
      example: must be a valid UUID
